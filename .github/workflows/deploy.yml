# multi-generated: github-workflow
# source: camera-roll-sounds-api/.github/workflows/deploy.yml
# repo: camera-roll-sounds-api
true:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: openbase-community/openbase-deploy@main
      with:
        heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
        heroku_email: ${{ secrets.HEROKU_EMAIL }}
        heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
        github_token: ${{ secrets.GH_PAT }}
    defaults:
      run:
        working-directory: camera-roll-sounds-api
